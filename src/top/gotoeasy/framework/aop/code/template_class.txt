package {pack};

import java.lang.reflect.*;
import java.util.*;

import top.gotoeasy.framework.aop.callback.*;

public class {simpleName}$$gotoeasy$$ extends {superClass} {

    private Map<String, Method>	map	= new HashMap<>(); // 方法缓存

    private Method getMethodByDesc(String desc){
        Method rs = map.get(desc);
        if ( rs == null ) {
            for ( Method th : {superClass}.class.getDeclaredMethods() ) {
                if ( th.toGenericString().equals(desc) ) {
                	rs = th;
                    map.put(desc, th);
                    break;
                }
            }
        }
        return rs;
    }

{field}

{method}

{callSuper}
}
