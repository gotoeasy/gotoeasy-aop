    @Override
    public {returnType} {methodName}({paramDefines}) {
        String desc = "{desc}";
        Method method = map.get(desc);
        if ( method == null ) {
            for ( Method th : {superClass}.class.getDeclaredMethods() ) {
                if ( th.toGenericString().equals(desc) ) {
                    method = th;
                    map.put(desc, method);
                    break;
                }
            }
        }

        {returnType} rs;
        try {
{beforeCode}
            rs = super.{methodName}({paramValues});
{afterCode}
            return rs;
        } catch (Exception e) {
{throwingCode}
            throw new RuntimeException(e);
        } finally {
{lastCode}
        }

    }
